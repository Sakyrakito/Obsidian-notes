```c++
int n;
cin >> n;
int l = 1, r = n, ans = 1;
while (l <= r) {
    int m = (l + r) / 2;
    
    if (m > n)
        r = m - 1;
    else {
        l = m + 1;
        ans = m;
    }
}
cout << ans << '\n';
```